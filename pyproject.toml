[project]
name = "parcel-bot"
version = "0.1.0"
description = "배송 접수 챗봇 — AI agent 블로그 시리즈 공용 데모"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2",
    "gunicorn>=23.0",
    "langgraph>=1.2.10",
    "uvicorn>=0.35",
]
